package org.firstinspires.ftc.teamcode;

import com.qualcomm.robotcore.eventloop.opmode.OpMode;
import com.qualcomm.robotcore.eventloop.opmode.TeleOp;

/* @TeleOp
public class HelloWorld extends OpMode { //Called when driver presses init
    @Override
    public void init() { // When driver presses init it will say "Hello World"
        int A = 10;
        int B = 5;
        boolean bVar = false;

        bVar = !bVar;

        if((A > 7) && (bVar)) {
            telemetry.addData("Both are TRUE", "A > 7 AND B < 7");
        }

        if((bVar) || (B > 7)) {
            telemetry.addData("One is TRUE", "A > 7 OR B < 7 OR Both");
        }

        if(!(bVar)) {
            telemetry.addData("It is TRUE", "A > 7 but will return FALSE");
        }

        if((A > 7) ^ (B < 7)) {
            telemetry.addData("Both are TRUE", "A > 7 AND B < 7 returns false but if one were true return TRUE");
        }
    }

    @Override
    public void loop() {
        
    }
} */
